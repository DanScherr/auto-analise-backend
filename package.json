{
  "name": "auto-analise-backend",
  "version": "1.0.0",
  "description": "This is the backend API fot the auto-analise-app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "SET NODE_ENV=production && node server",
    "dev": "nodemon server"
  },
  "author": "Daniel Scheicher",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.4.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
